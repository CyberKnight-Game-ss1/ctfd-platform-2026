---
- name: Configure VM1 - Web Server (AWS)
  hosts: vm1_web
  become: yes
  vars:
    ctfd_dir: /opt/ctfd
    # NOTE: Dùng PostgreSQL container thay vì RDS
    # Không cần SSM Parameter Store hay quyền RDS
    db_password: "ChangeMe_Strong_2026!"   # Đổi password này trước khi deploy!

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install required packages
      apt:
        name:
          - nginx
          - docker.io
          - docker-compose
          - python3-pip
          - unzip
        state: present

    - name: Install AWS CLI v2
      shell: |
        curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "/tmp/awscliv2.zip"
        unzip -q /tmp/awscliv2.zip -d /tmp/
        /tmp/aws/install
        rm -rf /tmp/aws /tmp/awscliv2.zip
      args:
        creates: /usr/local/bin/aws

    - name: Create CTFd directories
      file:
        path: "{{ item }}"
        state: directory
        owner: "1001"
        group: "1001"
        mode: '0755'
      with_items:
        - "{{ ctfd_dir }}"
        - "{{ ctfd_dir }}/themes"
        - "{{ ctfd_dir }}/uploads"    # QUAN TRỌNG: phải thuộc UID 1001 (user ctfd trong container)
        - "{{ ctfd_dir }}/logs"
        - "{{ ctfd_dir }}/redis"
        - "{{ ctfd_dir }}/postgres"   # Volume cho PostgreSQL container

    - name: Copy custom themes
      copy:
        src: ../../themes/
        dest: "{{ ctfd_dir }}/themes/"

    - name: Create docker-compose.yml cho CTFd + Redis + PostgreSQL
      copy:
        dest: "{{ ctfd_dir }}/docker-compose.yml"
        content: |
          version: '3'
          services:

            # ============================================================
            # PostgreSQL - thay thế RDS, chạy trực tiếp trên VM
            # ============================================================
            db:
              image: postgres:15-alpine
              restart: always
              environment:
                - POSTGRES_USER=ctfd
                - POSTGRES_PASSWORD={{ db_password }}
                - POSTGRES_DB=ctfd
              volumes:
                - {{ ctfd_dir }}/postgres:/var/lib/postgresql/data
              healthcheck:
                test: ["CMD-SHELL", "pg_isready -U ctfd"]
                interval: 10s
                timeout: 5s
                retries: 5

            ctfd:
              image: ctfd/ctfd:latest
              restart: always
              ports:
                - "8000:8000"
              environment:
                - SECRET_KEY={{ ctfd_secret_key | default('change_me_in_production') }}
                - UPLOAD_FOLDER=/var/uploads
                - DATABASE_URL=postgresql://ctfd:{{ db_password }}@db:5432/ctfd
                - REDIS_URL=redis://cache:6379
                - WORKERS=4
                - LOG_FOLDER=/var/log/CTFd
                - ACCESS_LOG=-
                - ERROR_LOG=-
                - REVERSE_PROXY=True
              volumes:
                - {{ ctfd_dir }}/logs:/var/log/CTFd
                - {{ ctfd_dir }}/uploads:/var/uploads
                - {{ ctfd_dir }}/themes/ctfd-theme-neubrutalism:/opt/CTFd/CTFd/themes/ctfd-theme-neubrutalism
              depends_on:
                db:
                  condition: service_healthy
                cache:
                  condition: service_started

            cache:
              image: redis:4
              restart: always
              volumes:
                - {{ ctfd_dir }}/redis:/data

            worker:
              build: .
              user: root
              restart: always
              command: ["opt/CTFd/ping.sh", "redis:6379", "worker"]
              environment:
                - SECRET_KEY={{ ctfd_secret_key | default('change_me_in_production') }}
                - UPLOAD_FOLDER=/var/uploads
                - DATABASE_URL=postgresql://ctfd:{{ db_password }}@db:5432/ctfd
                - REDIS_URL=redis://cache:6379
                - WORKERS=1
                - LOG_FOLDER=/var/log/CTFd
                - ACCESS_LOG=-
                - ERROR_LOG=-
                - REVERSE_PROXY=True
              volumes:
                - {{ ctfd_dir }}/logs:/var/log/CTFd
                - {{ ctfd_dir }}/uploads:/var/uploads
              depends_on:
                - ctfd
                - cache

    - name: Start CTFd via docker-compose (PostgreSQL chạy cùng stack)
      shell: "docker-compose up -d"
      args:
        chdir: "{{ ctfd_dir }}"

    - name: Configure Nginx as Reverse Proxy
      copy:
        dest: /etc/nginx/sites-available/default
        content: |
          server {
              listen 80 default_server;
              client_max_body_size 200M;

              # Proxy to CTFd
              location / {
                  proxy_pass http://127.0.0.1:8000;
                  proxy_set_header Host $host;
                  proxy_set_header X-Real-IP $remote_addr;
                  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                  proxy_set_header X-Forwarded-Proto $scheme;
              }
          }

    - name: Restart Nginx
      service:
        name: nginx
        state: restarted
        enabled: yes

    # Amazon CloudWatch Agent - thay thế Google Cloud Ops Agent
    # Đẩy Nginx logs, Docker logs về CloudWatch Logs
    - name: Install Amazon CloudWatch Agent
      shell: |
        wget -q https://s3.amazonaws.com/amazoncloudwatch-agent/ubuntu/amd64/latest/amazon-cloudwatch-agent.deb -O /tmp/amazon-cloudwatch-agent.deb
        dpkg -i /tmp/amazon-cloudwatch-agent.deb
        rm -f /tmp/amazon-cloudwatch-agent.deb
      args:
        creates: /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent

    - name: Configure CloudWatch Agent
      copy:
        dest: /opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.json
        content: |
          {
            "logs": {
              "logs_collected": {
                "files": {
                  "collect_list": [
                    {
                      "file_path": "/var/log/nginx/access.log",
                      "log_group_name": "/ctfd/nginx/access",
                      "log_stream_name": "{instance_id}"
                    },
                    {
                      "file_path": "/var/log/nginx/error.log",
                      "log_group_name": "/ctfd/nginx/error",
                      "log_stream_name": "{instance_id}"
                    },
                    {
                      "file_path": "/opt/ctfd/logs/*.log",
                      "log_group_name": "/ctfd/application",
                      "log_stream_name": "{instance_id}"
                    }
                  ]
                }
              }
            }
          }

    - name: Start CloudWatch Agent
      shell: >
        /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl
        -a fetch-config -m ec2 -s
        -c file:/opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.json
